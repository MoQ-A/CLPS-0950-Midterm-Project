% Message Window
% msgbox('Enter values into command line.', 'Popup Window', 'help');

myicon = imread('playbutton.png');
f = msgbox('Start', "Custom", myicon);

%UI Implementation: Starting Menu
menu_title = "Game of Life";
menu_opts = ["Start" "Explanation" "Next" "Clear"];
choice = menu(menu_title,menu_opts);
%UI implementation for counting the # of cells populated



%UI: Explanation-Rules button?

%UI 

%UI  Counting the number of Generations a frame is on & display.
 
generation = msgbox('Round', '1' );
while generation_count > 1
    generation = msgbox('Round', count(generation_count));
end

%UI Implementation of button to Reset (From Beginning)




